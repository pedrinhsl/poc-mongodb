Simple Crud POC to test MongoDB

steps to run:

put your connection string generated on mongodb in your .env like this:

```
CONNECTION_STRING=
```

after that run:

```
npm install
```

and to run app:

```
npm serve
```
